{
  "buildCommand": "pip install -r requirements.txt",
  "builds": [
    {
      "src": "app/api/cron/route.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/cron",
      "dest": "app/api/cron/route.ts"
    }
  ]
}